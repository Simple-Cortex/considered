An empty static starter: one HTML shell with `<main id="app">`, a CSS reset, and an empty ES module.

Serve it from this directory with any static server, for example `npx serve .` or `python3 -m http.server`, then open the root URL.
