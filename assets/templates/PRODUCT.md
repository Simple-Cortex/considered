# Product context

Captured by `init`. This is product-level context shared by all surfaces. Keep it short and factual. Surface-specific reasoning belongs in `FRAME.md`.

**Status:** draft | current
**Workspace root:** `<workspace-relative root>`
**Updated:** `<date or revision>`
**Evidence checked:** `<brief, source paths, research, or none>`

## Product and delivery

**Product or service:**
**Requested outcome:**
**Target platform and viewport:**
**Source or delivery target:**
**Existing system to preserve:** tokens, components, conventions, or `none found`

## Users

**Primary role:**
**Expertise:** novice | intermediate | expert
**Context of use:** device, environment, competing attention
**Frequency and trigger:**

## Decision context

> `<role>` decides `<what>` within `<time budget>`, using `<inputs>`;
> being wrong costs `<consequence>`, and this occurs `<frequency>`.

**Current workaround:**
**Observable successful outcome:**

## Voice and boundaries

**Register:**
**Never sounds or looks like:**
**Anti-references:** concrete patterns, products, or visual cliches to refuse
**Out of scope:**

## Constraints

| Area | Known constraint | Evidence | Effect on design |
| --- | --- | --- | --- |
| Data |  |  |  |
| Platform |  |  |  |
| Performance |  |  |  |
| Brand |  |  |  |
| Compliance or accessibility |  |  |  |
| Delivery |  |  |  |

## Assumptions

Record nonblocking gaps here or in the active `FRAME.md`. Do not turn these into questions unless they meet the blocking test in `references/workflow.md`.

| ID | Assumption | Why it is safe to proceed | Impact if wrong | Validation path |
| --- | --- | --- | --- | --- |
| A1 |  |  |  |  |

## Blocking facts

Use only for facts that require a user answer before the affected command can continue. Write `None` when there are no blockers.

| ID | Missing fact | Why it blocks | Exact question | Affected command |
| --- | --- | --- | --- | --- |
| B1 |  |  |  |  |
